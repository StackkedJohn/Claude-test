FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Install additional development tools
RUN npm install -g nodemon @types/node

# Expose port
EXPOSE 3000

# Development command with hot reload
CMD ["npm", "start"]